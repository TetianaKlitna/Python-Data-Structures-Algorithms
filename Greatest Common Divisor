import math 

def findGCD(a, b):

  if a == b:
    return a

  res = min(a, b)
  
  while res > 0:
    if a % res == 0 and b % res == 0: 
      break

    res -= 1

  return res

resGCD = math.gcd(36,60)
print(resGCD)
